describe("Create new invoice", () => {
  /* TODO: Add tests for required fields in an appropriate tests */
  /* TODO: Add tests for server side — contract date can not be less than invoice date */
  it("If all fields are not empty, 'Save' button should be enabled", async () => {});
  it("If invoice name is empty, 'Save button' should be disabled", async () => {});
  it("If invoice date is empty, 'Save button' should be disabled", async () => {});
  it("If contract name is empty, 'Save button' should be disabled", async () => {});
  it("If contract date is empty, 'Save button' should be disabled", async () => {});
  it("If invoice has no related contractor, 'Save button' should be disabled", async () => {});
  it("If invoice has no related client, 'Save button' should be disabled", async () => {});
  it("If no service was added to invoice, 'Save button' should be disabled", async () => {});
  it("If terms are empty, 'Save' button should be enabled", async () => {});
});
