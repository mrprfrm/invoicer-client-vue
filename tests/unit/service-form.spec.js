describe("Create new service", () => {
  it("If all fields are not empty, 'Save' button should be enabled", async () => {});
  it("If service description is empty, 'Save button' should be disabled", async () => {});
  it("If service quantity is empty, 'Save button' should be disabled", async () => {});
  it("If service price is empty, 'Save button' should be disabled", async () => {});
  /* TODO: add default value for amount equals 1 */
  it("If service amount is empty, 'Save button' should be disabled", async () => {});
});
